@page
@model presentacion.Pages.EditRevOpeModel
@{
}
<head>
    <link rel="stylesheet" href="~/css/TipoVehiculo.css" />
</head>

    @if (@Model.EditRevOper.RevOperacionesId > 0 )
    {
        <ol class="breadcrumb">
            <li class="breadcrumb-item active">Sección Actualizar Rev-Operaciones</li>
        </ol>
        <h3>Modificar Datos</h3>
    }
    else
    {
        <ol class="breadcrumb">
            <li class="breadcrumb-item active">Sección Agregar Rev-Operaciones</li>
        </ol>
        <h3>Creación de Rev-Operacion</h3>
    }

 <div class="container" id="formList">
    <form method="post"  class="needs-validation" novalidate>
        <input type="hidden" asp-for="EditRevOper.RevOperacionesId"/>

        <div class="row col-md-10">
            <div class="col-md-2">
                <label class="col-form-label">Operacion</label>
            </div>
            <div class="col-md-3">
                <select  class="form-select" asp-for="EditRevOper.OperacionId" required>
                    @foreach (var operaciones in Model.operacion)
                    {
                        <option value="@operaciones.OperacionId">@operaciones.Nombre</option>
                    }
                </select>
                <div class="invalid-feedback">
                    Por favor Inserte una Operacion
                </div>
            </div>
    
            <div class="col-md-2">
                <label class="col-form-label">Revision</label>
                </div>
            <div class="col-md-3 col-lg-3">
                <select  class="form-select" asp-for="EditRevOper.RevisionId" required>
                    @foreach (var revisiones in Model.revision)
                    {
                        <option value="@revisiones.RevisionId">@revisiones.vehiculo.Placa</option>
                    }
                </select>
                <div class="invalid-feedback">
                    Por favor Inserte una Revision
                </div>
            </div>
        </div>
        <br>
      
        <div class="col-md-5">
            <button type="submit" class="btn btn-outline-primary">Guardar</button>
        </div>
           
    </form>
</div> 


<script src="~/js/Persona.js"></script>